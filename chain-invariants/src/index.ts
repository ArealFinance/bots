/**
 * Phase 22: chain-invariants exporter — entrypoint stub.
 *
 * Replaced in C6 with the full poller + metrics server. C1 ships only the
 * scaffold so workspace install + tsc --noEmit can pass without dragging
 * the C2-C5 deltas in atomically.
 */

import { logger } from '@areal/bots-shared';

logger.info('chain-invariants stub starting (Phase 22 C1 scaffold)');
logger.info('full poller lands in C6 — exiting');
process.exit(0);
