{
  "name": "chain-invariants",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 22: read-only on-chain invariant exporter — polls 4 invariants (merkle root age, NAV age, authority drift, RWT supply parity), exposes them as Prometheus metrics on 127.0.0.1:9201, and serves shields.io endpoint badges for the public README",
  "license": "Apache-2.0",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@areal/bots-shared": "*",
    "@areal/sdk": "file:../../sdk",
    "@solana/web3.js": "^1.95.0",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
